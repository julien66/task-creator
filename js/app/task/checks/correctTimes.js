/**
 * @file
 * first turnpoint check for the task creator
 */
define([], function() {
  var run = function(turnpoint, turnpoints) {

    return {
      advice : false,
    }
  }

  return {
    run: run,
  }
});
